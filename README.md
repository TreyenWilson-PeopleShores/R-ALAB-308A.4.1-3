# R-ALAB-308A.4.1

You have to run:
- npm init -y
- npm install bootstrap

To run the the fecth website type "npm start" into the terminal.
To run the Axios version, open the Axios-Version folder, and type "npm start".